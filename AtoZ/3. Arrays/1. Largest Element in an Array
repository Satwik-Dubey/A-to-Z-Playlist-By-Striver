class Compute {
    
    public int largest(int arr[], int n)
    {
       int  w=Integer.MIN_VALUE;
        for(int i=0;i<n;i++){
            if(w<arr[i]){
                w=arr[i];
                
            }
        }
        return w;
    }
}
